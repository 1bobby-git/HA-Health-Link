# HealthLink v0.3.8 — 공통 UI·네이티브 헤더·아이콘 개선

## 적용 내용

- 이전에 로컬 패치로 제공한 공통 웹 UI를 Studio에 적용합니다. 첨부한 `HA_Component_Web_UI_Design_System_v1.0.md` 기준의 서체, 헤더, 여백, 카드와 버튼 규격을 사용합니다.
- HA가 제공하는 `narrow`, 사이드바 숨김, 키오스크 상태에 따라 실제 `ha-top-app-bar-fixed`를 표시합니다. 컴포넌트 폭 870px을 HA 헤더 표시 기준으로 하드코딩하지 않습니다. 별도의 가짜 제목 바나 중복 메뉴는 만들지 않습니다.
- 설정을 톱니바퀴, 새로고침을 회전 화살표로 표시합니다. 추가·닫기·이동·보호·맥박 아이콘도 같은 MDI SVG 방식으로 통일합니다. 아이콘 20px, 조작 영역 44px, 접근 가능한 이름과 헤더 툴팁을 적용합니다.
- 외부 아이콘 폰트나 아이콘 로더에 의존하지 않습니다. 라이선스와 출처를 번들에 포함합니다.
- Studio의 건강 항목 목록·검색·센서 만들기·기록 조회에 한글 이름을 적용합니다. 원본 ID와 데이터 출처는 그대로 보존합니다.
- 연결 화면에 심전도(ECG) 가져오기 단계별 안내를 추가하고 건강 데이터 화면에서 바로 열 수 있게 합니다. 가져오기와 민감 자료 허용은 기존 HA 설정에서 사용자가 직접 선택합니다.

## 유지 범위와 제한

공식 원본 로고, 흰색 요약 카드, 모바일 안전 영역, 기존 프로필·엔티티 ID, 센서 값·단위, 건강 저장소, 관리자 권한과 민감 자료 정책은 유지합니다. 이번 한글 표시 변경은 Studio 프런트엔드에 적용하며, HA 엔티티의 기본 이름을 바꾸는 추가 Python 변경은 포함하지 않습니다.

## 검증

로컬 Chromium에서 기존 UI 26개, 공통 헤더·아이콘 12개, 원본 로고·대비 8개, 안전 영역 8개를 각각 실행해 통과했습니다. 네이티브 헤더 부분은 테스트용 대체 구현과 HA 속성으로 검증했으며 실제 HA 또는 iPhone·Safari·VoiceOver에서 검증한 결과는 아닙니다. 프런트엔드 모듈·원본 PNG는 실제 배포 파일을 사용했습니다. 최종 Python·Node·HACS·Hassfest CI는 이 버전의 PR 체크를 참고하세요.

## 업데이트

HACS에서 v0.3.8 다운로드 → Home Assistant 재시작 → Studio 다시 열기. iPhone은 HA 앱을 완전히 종료한 뒤 다시 여세요. PC에서 이전 화면이 남으면 Ctrl+F5로 새 파일을 불러오세요. 통합을 삭제하거나 프로필을 다시 등록하지 마세요.

GitHub 릴리스 배포와 사용자 HA의 실제 설치·재시작은 별개입니다.
