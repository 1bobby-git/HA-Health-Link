name: Apply HealthLink v0.2.1
on:
  push:
    branches: [healthlink-v0.2.1-native-options-duplicates]
    paths:
      - '.healthlink-maintenance/**'
      - '.github/workflows/apply-v021.yml'
permissions:
  contents: write
jobs:
  apply-and-verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
        with:
          ref: healthlink-v0.2.1-native-options-duplicates
      - uses: actions/setup-python@v6
        with:
          python-version: '3.14'
      - uses: actions/setup-node@v5
        with:
          node-version: '24'
      - run: pip install 'homeassistant==2026.9.1' pytest ruff
      - name: Apply patch
        run: python .healthlink-maintenance/apply_v021.py
      - name: Validate Python
        run: |
          ruff check --select E9,F63,F7,F82 custom_components tests
          python -m compileall -q custom_components/health_link
          python -m pytest -q
      - name: Validate frontend
        run: |
          node --check custom_components/health_link/frontend/health-link-panel.js
          node --test tests/test_panel.cjs
      - name: Commit verified implementation
        run: |
          rm -rf .healthlink-maintenance .github/workflows/apply-v021.yml
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add -A
          git commit -m 'fix: keep duplicate health entities and restore native settings (v0.2.1)'
          git push origin HEAD:healthlink-v0.2.1-native-options-duplicates
