# HealthLink v0.3.7 — 모바일 헤더 안전 영역 수정

## 수정

- iPhone 상태 표시줄·다이내믹 아일랜드 아래로 헤더가 겹치는 문제를 수정했습니다.
- HA가 전달하는 상하 안전 영역을 스크롤 바깥에서 확보합니다. 화면을 스크롤하거나 가로·세로로 전환해도 안전 영역이 유지됩니다.
- 좌우 여백은 HA 사이드바가 이미 처리한 안전 영역을 중복 적용하지 않습니다.
- 센서 입력 패널의 제목·닫기·저장 버튼에도 화면 안전 영역을 적용합니다.
- 320px 다크 화면에서 설정 버튼이 다음 줄로 밀리던 여백 문제를 수정했습니다.
- 승인된 원본 로고, 흰색 요약 카드, 건강 데이터와 기존 기능은 유지합니다.
- 프런트엔드 진입 모듈과 변경된 스타일 모듈의 캐시 식별자를 함께 갱신했습니다.

## 업데이트

HACS에서 HealthLink v0.3.7을 다운로드한 뒤 Home Assistant를 재시작하세요. iPhone에서는 HA 앱을 완전히 종료했다가 다시 열어 새 화면을 불러오세요. 통합 삭제·프로필 재등록은 필요하지 않습니다.

## 검증

합성 HA API와 안전 영역 값을 사용하는 Chromium 브라우저 테스트 34개(기존 동작 26개 + 안전 영역 8개), 로고·레이아웃 Node 검사 7개가 로컬에서 통과했습니다. 전체 저장소 CI 결과는 이 버전의 PR/커밋 체크를 참고하세요. 실제 iPhone·Safari·VoiceOver에서 실행한 테스트는 아닙니다. 배포와 사용자 HA 원격 설치·재시작은 별개입니다.
